# Face Detection Feature - Installation Required

## ⚠️ Important Notice

The Face Detection feature requires the `face-api.js` npm package to be installed, but the package.json file is currently read-only in this template.

## Required Steps to Enable Face Detection

### 1. Install face-api.js Package

The following dependency needs to be added to `frontend/package.json`:

